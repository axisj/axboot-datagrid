import * as React from 'react';
import styled from '@emotion/styled';

interface Props {}

function TableHeadFrozen(props: Props) {
  return <Container>TableHeadFrozen</Container>;
}

const Container = styled.div``;

export default TableHeadFrozen;
