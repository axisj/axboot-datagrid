import * as React from 'react';
import styled from '@emotion/styled';

interface Props {}

function TableBodyFrozen(props: Props) {
  return <Container>TableBodyFrozen</Container>;
}

const Container = styled.div``;

export default TableBodyFrozen;
